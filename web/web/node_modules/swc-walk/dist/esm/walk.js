import { simple as acornSimpleWalk, ancestor as acornAncestor } from 'acorn-walk';
import { BaseVisitor } from './baseVisitor.js';
export function simple(ast, visitors, baseVisitor = new BaseVisitor(), state) {
    // @ts-expect-error (acorn-walk ast nodes have start/end instead of span)
    acornSimpleWalk(ast, visitors, baseVisitor, state);
}
export function ancestor(ast, visitors, baseVisitor = new BaseVisitor(), state) {
    // @ts-expect-error (acorn-walk ast nodes have start/end instead of span)
    acornAncestor(ast, visitors, baseVisitor, state);
}
